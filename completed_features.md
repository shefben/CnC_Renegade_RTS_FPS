# Completed Features

One or two sentences per finished item. Detail lives in `docs/`.

---

## P00-BUILD: OpenW3D baseline frozen and building

Froze the pre-TT baseline at `b970f592` and got green x86 builds of client, dedicated
server, and tools plus 2/2 CTest; x64 retained as secondary. Details in
`docs/integration/OpenW3DBaseline.md`, `BaselineKnownIssues.md`,
`BaselineScriptCatalog.txt` (1747 stock scripts).

## W-BOOTSTRAP: project-state files established

Created and reconciled the four state files against repository reality, and raised
and closed Q-001 through Q-004.

## N/A: fetch latest `w3dhub/OpenW3D/main` (P00, roadmap Section 6)

Not applicable — user directed freezing on the present codebase (`b970f592`) and
ignoring the roadmap's audited SHA. See Q-003.

## N/A: obtain/verify official `source-4.8.4.zip` (P01, roadmap Section 7)

Not applicable — no official archive was supplied, so "download `source-4.8.4.zip`",
"verify MD5 exactly", "preserve the untouched archive", and "extract into
`third_party/tt484_source_reference/`" cannot be performed. The TT donor is
identified by directory contents plus `TT_VERSION 4.8f`; recorded in
`docs/tt484/TTParityMatrix.md` §1.

## P01-INVENTORY: TT source, script and class inventory

Inventoried 1605 TT files (1115 classified), diffed the script catalogs (2155 TT vs
1747 stock, 13 replacements, so the Phase 4 registry is 3889), and analysed the
binary surface. Deliverables in `docs/tt484/`.

## P01-HOOKMAP: every TT hook site mapped to an OpenW3D owner

Covers the backlog line "Identify every binary-address hook and locate the
corresponding OpenW3D source call site." Rebuilt the extractor after finding the
earlier 151-site count missed TT's `hookAs*` macro family — the real figure is 760
sites, 656 in scope. All 656 now resolve: 644 to an OpenW3D file (438 high
confidence, exact symbol match) and 12 marked DROP; none unmapped. Evidence:
`docs/tt484/TTHookSites.tsv`, parity matrix §4.

## P01-CLASSDELTA: declaration deltas for the 22 roadmap Section 7 classes

Covers the backlog line "Compare TT public class declarations with OpenW3D's real
classes." Result: 14 merge, 1 naming fix (`SamSiteGameObj`/`SAMSiteGameObj`), 7
genuinely new. Evidence: `docs/tt484/TTClassDeltas.md`, parity matrix §5.5.

## P01-TOOLS: analysis generators committed

`tools/tt484/` reproduces both deliverables from source deterministically, with a
README recording why the hook extractor must know the macro primitives.

## P01-FILEMAP: native destination for every TT source file — P01 COMPLETE

Extended the inventory from the six engine areas (1115 files) to the whole tree
(1607) and added `native_destination` / `dest_method` / `dest_confidence`: 1141
files land in an OpenW3D directory, 466 carry an explicit exclusion/drop/split,
none are unaccounted for (1411 high confidence, 73 low). Dispositioned the four
open areas — `MemoryManager/`→`Code/wwlib`, `VFS/`→DROP with its `FileClass`
consumer seam to `Code/wwlib`, `teamspeak/`→DROP, `bansystem/`→`Code/wwnet`
deferred to P44/P47. This closes the P01 acceptance wording and the WIP entry
"P01: Import and inventory TT 4.8.4 — final gap"; evidence in
`docs/tt484/TTFileInventory.tsv` and parity matrix §2.2/§2.3/§6, regenerated by
`tools/tt484/files2.py`.

## P02-SCOPE: resolve where every TT-only method body actually is

`tools/tt484/defcheck.py` + `counterpart.py` classify all 227 TT-only methods on
the 22 roadmap Section 7 classes: 100 mergeable TT code, 9 address stubs over
stock behaviour, 57 already owned by another OpenW3D class, 58 to be written
(`docs/tt484/TTMethodSources.tsv`, parity matrix 5.6). Corrected two matrix
claims — the "no stock predecessor" line in 5.2, and the NEW disposition for
`AirFactoryGameObj`/`NavalFactoryGameObj`, which are `VehicleFactoryGameObj`
subclasses.

## Q-005: repository under local version control

All previously untracked work committed on `main` in four commits (`ce4c3b75`
analysis + state files, `cdd1ed2a` TT 4.8.4 and the pinned `GeneralsGameCode`
submodule, `0a191e58` the standalone `Code/` side projects, `41164d71` the ~900 MB
`models/` art); local agent/tool caches gitignored. No remote is under our control,
so nothing is pushed.


## P02-A: TT SoldierGameObj, VehicleGameObj and BuildingGameObj members merged

51 TT-only members merged into the three canonical classes, each flag wired to
the engine site that enforces it rather than left as inert state — freeze and
blocked-action-key in `Apply_Control`, vehicle-theft permission split across
`Is_Permitted_To_Enter_Vehicle` and `Is_Entry_Permitted`, replicated model scale,
muzzle-direction override, stealth-while-empty, and a building bounding box unioned
in `Collect_Building_Components`. All saved, replicated and defaulted to stock
behaviour; `combat` links clean. Delta 227 -> 176 (`6b46638a`, `3d4d6bb9`,
`cfd49a95`).

## P02-B: TT NetworkObjectClass members merged, hint loop closed

`Set_Is_Delete_Pending`, `Set_Dirty_Bits` and the per-object hint timestamp
merged. The timestamp closes a real hole: `cClientHintManager` tested when the
server last *sent* an object, not when the client last *asked*, so an object the
server could not satisfy was re-hinted every second forever. `renegade.exe` links
clean (`58d62db7`).

## P02-C: TT BuildingGameObj geometry queries and the building revival path

`Building_In_Range`, `Is_In_Range_Coarse`, `Cast_Ray` and the closest-polygon
position overloads written from the declared semantics on top of the bounding box
from P02-A; these are the primitives matrix 5.5 flags as the prerequisite for
P33/P37/P38. `On_Revived` and a routed `Set_Is_Destroyed` give buildings a
reversible destroyed state, with inverses on the four subclasses that have them.
`combat` links clean; `BuildingGameObj` and `SoldierFactoryGameObj` are clear
(`50d728cc`).

## P02 tooling: wrapped-declaration parsing in the TT survey generators

`ttparse` matched one physical line at a time, so wrapped signatures parsed as
fragments and members OpenW3D already had read as TT-only. Joining logical lines
removed five false positives and recovered two hidden TT declarations; matrix 5.7
records it (`a1d601e0`, and the keyword-prefix guard in `50d728cc`).

## N/A: `NetworkObjectClass::Set_Object_Dirty_Bitx`

Not merged. A non-virtual duplicate of `Set_Object_Dirty_Bit` existing so a DLL
hook could reach the unhooked original; nothing overrides that virtual in OpenW3D
and directive 0.4 forbids reintroducing the hook architecture.

## N/A: `RenderObjClass::Register_For_Rendering` and `Set_User_Lighting_Flag`

Deferred, not merged. Both are declaration-only with no body anywhere in the
donor. `Register_For_Rendering(bool hint_static)` belongs to TT's renderer
rework, and `shaders/` is excluded by directive 0.6; `Set_User_Lighting_Flag`
is a vertex-solve helper whose "appropriately" contract is only recoverable
alongside the static-lighting work.
