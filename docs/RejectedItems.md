# Rejected, declined and not done

Every item this project decided **not** to do, with the reason, by phase.

Generated by `tools/rejected_items.py` from the files the phases actually write:
`completed_features.md`, `docs/tt484/TTHookSites.tsv`,
`docs/zerohour/ZeroHourSurfaceClassification.tsv` and
`docs/zerohour/OpenW3DPortMatrix.tsv`. Editing this file by hand loses the edit
on the next regeneration -- change the source, or add to `PHASE_DECISIONS` in
the tool. Re-run it after any pass that declines something.

A rejection here is a decision, not an oversight. Something genuinely unfinished
lives in `unstarted_features.md` or `WIP_features.md` instead.

## Standing rules that reject things in every phase

**0.4  one canonical implementation.** Where TT patched, hooked, overrode or replaced stock behaviour, the TT result is merged into the canonical OpenW3D owner and the superseded path is deleted. Rejected by this rule: every "keep both and select with a flag" arrangement, stock-vs-TT wrappers, and "EA behaviour mode" / "TheSuperHackers behaviour mode" switches on donor code.

**0.5  no DLL-hook or proxy architecture.** The historical TT arrangement -- a patched Game.exe, a proxy DLL and scripts2.dll -- is not reintroduced, and neither is a binary plugin compatibility layer. Rejected by this rule: TT's injection scaffolding, its plugin hook family, and the byte patches whose only purpose was to reach an injected DLL.

**0.6  shaders/ is out of scope.** The TT shaders.dll renderer replacement is not ported. Rejected by this rule: the renderer, texture, shadow, dazzle, vertex-buffer and Bink hook families that exist only to route drawing through shaders.dll.

**Zero Hour donor shape.** The donor is TheSuperHackers `GeneralsGameCode` configured as `Core + GeneralsMD`. Rejected: `Generals/` as a donor stage or predecessor implementation source, and any plan to build or stage through a predecessor-game implementation first. Two tools error if a `Generals/` path reaches an inventory.

**Physics and world geometry.** OpenW3D/WWPhys is kept; no Jolt or replacement-physics migration. Renegade FPS world geometry and interiors are preserved. Rejected: replacing authored W3D world geometry with a heightmap outright (see ZH-04), and any second unrelated world database (see ZH-19).

**Stock asset compatibility.** Renegade models, maps and textures load unmodified. Engine work may augment, fix and extend them, never require re-authoring or re-export. Rejected: any change whose correctness depends on content being rebuilt.

## P00 through P06 -- TT 4.8.4

### Items recorded N/A in `completed_features.md`

18 entries.

| Phase | Item | Why not |
| --- | --- | --- |
| P00 | fetch latest `w3dhub/OpenW3D/main` (P00, roadmap Section 6) | Not applicable — user directed freezing on the present codebase (`b970f592`) and ignoring the roadmap's audited SHA. See Q-003. |
| P01 | obtain/verify official `source-4.8.4.zip` (P01, roadmap Section 7) | Not applicable — no official archive was supplied, so "download `source-4.8.4.zip`", "verify MD5 exactly", "preserve the untouched archive", and "extract into `third_party/tt484_source_reference/`" cannot be performed. The TT donor is identified by directory contents plus `TT_VERSION 4.8f`; recorded in `docs/tt484/TTParityMatrix.md` §1. |
| P02 | `NetworkObjectClass::Set_Object_Dirty_Bitx` | Not merged. A non-virtual duplicate of `Set_Object_Dirty_Bit` existing so a DLL hook could reach the unhooked original; nothing overrides that virtual in OpenW3D and directive 0.4 forbids reintroducing the hook architecture. |
| P02 | `RenderObjClass::Register_For_Rendering` and `Set_User_Lighting_Flag` | Deferred, not merged. Both are declaration-only with no body anywhere in the donor. `Register_For_Rendering(bool hint_static)` belongs to TT's renderer rework, and `shaders/` is excluded by directive 0.6; `Set_User_Lighting_Flag` is a vertex-solve helper whose "appropriately" contract is only recoverable alongside the static-lighting work. |
| P02 | TT `ExtendedNetworkObject` | Not ported. Its stated purpose is adding virtuals to `NetworkObjectClass` without changing base-class offsets -- the class-layout ABI constraint the P02 backlog lines "Do not preserve obsolete class-layout ABI constraints" and "Do not introduce external-plugin facade headers/adapters" forbid. Its one virtual went on `NetworkObjectClass` directly (see P02-G); those two constraint lines are discharged by that decision. |
| P02 | `VehicleGameObj::Export_Occasional` / `Import_Occasional` | Superseded. The state TT carried in the occasional tier -- `CanDrive`, `AllowStealthWhileEmpty`, `CanBeStolen`, `LockTeam`, `ScriptsVisible` -- already replicates through `Export_Rare`, and `CanFire` through the frequent tier. Matrix 5.8. |
| P02 | `BuildingGameObjDef::Dump` and `AABTreeCullSystemClass::Collect_Branch` | `Dump` is `#ifdef DDBEDIT` in TT's own header: an editor-only preset dump with no runtime behavior and no matching OpenW3D build. `Collect_Branch` is OpenW3D's existing `Collect_Objects_Recursive(AABTreeNodeClass *)` under another name. Matrix 5.8. |
| P02 | TT `AirFactoryGameObj` | Superseded by `AirStripGameObj`, which already owns the vehicle/purchaser/busy machinery and the drop-off cinematic that TT's def renames. `defcheck.py` reaches the same answer, marking its rows "owned elsewhere" and naming the airstrip first. `BuildingGameObj::As_AirFactoryGameObj` is N/A with it -- `As_AirStripGameObj` is the same cast. Matrix 5.9. |
| P02 | TT `NavalFactoryGameObj` -- reassigned to Zero Hour Feature 7 | Roadmap Section 22 lists "TT naval factories" under Feature 7 alongside the water surface and amphibious/naval physics its spawn zone needs. Its 27 rows and `BuildingGameObj::As_NavalFactoryGameObj` carry there. Matrix 5.9. |
| P03 | `UDP6`/`UDP7`/`UDP8` | Land in the GameSpy query-and-reporting SDK statically linked into the stock binary, which is not part of this tree and whose master server is gone. `UDP8` is not a bounds fix at all -- it forces the `;`-prefixed remote command dispatch through `vtbl+0xFC` to be skipped unconditionally. |
| P03 | `Do not load all .mix files at startup` | Declined with reason. `WriteJump(0x0043907C, 0x00439192)` does not trim the `data/*.mix` scan -- it jumps the whole file-factory mount block because TT substitutes its own VFS. That is TT infrastructure, not a stock fix, and OpenW3D keeps its own file factory (`Code/Commando/init.cpp:766-791`). |
| P03 | `radar fix` | The patch is inert. `Enable_Radar_Patch` re-asserts `base->Enable_Radar(base->Is_Radar_Enabled())` through a `RENEGADE_FUNCTION` bound to stock `0x006EFD00`, which opens `cmp [esi+6D7h], al / jz` on that same `IsRadarEnabled` field -- so the value is compared against itself and the setter returns. Nothing to merge. Recorded with its evidence so the row is not reopened. |
| P03 | NeutralVehiclePointsFix | The flag is declared in TT 4.8.4 and never read. Its mechanism is the scripts team override (`SCRIPTS_LastTeam`, set by `Set_Vehicle_Team`), declined under directive 0.5, and canonical `Do_Damage` already awards no points for a neutral target and negative points for a teammate, which is TT's non-scripts path. |
| P03 | VehicleBuildingDisable and the TT sidebar option group | `VehicleBuildingDisable` is a second production model -- per-team rather than per-factory -- selected by a flag, which directive 0.4 forbids; its `CurrentlyBuilding` state only travels over the scripts text channel, and the roadmap's Commander production model supersedes a team-wide lock. It takes the PT "building" message with it. `RefillLimit`, `AlternateSelectEnabled` and the sidebar sound and texture names belong to TT's own `SidebarDlg`, superseded by the roadmap's Commander sidebar. `ScrollingRadarMap` and `ModRegistryKey` only move data over the scripts channel. |
| P03 | two byte patches with no source counterpart | `emoticons fix` and `start button bug fix` are raw bytes written at stock addresses with nothing anywhere in TT's source to explain them. Identifying their canonical owners would need the shipped binary, which is out of bounds. |
| P03 | TT's injection scaffolding | The `remove call to ...` rows, `nop out WWPhys::Shutdown call`, `stop FDS from loading at startup`, `unload level hook` and TT's `Start_Application` all exist so a DLL loaded into a running exe can re-run initialisers under its own control. Natively those initialisers stay put and read the merged settings. |
| P04 | the plugin hook family in the in-scope donor library | `AddChatHook`, `AddPlayerJoinHook`, `AddThinkHook` and nineteen more (26 calls across the 25 files) exist so a binary plugin can install a callback into a running exe, which directive 0.5 forbids. Natively the same notifications come off `GameEventBus`; see `docs/tt484/NativeEventDispatch.md`. The five `REF_DECL` data bindings go the same way. |
| P04 | controller behavior where supported. | There is no controller support in this tree to have broken -- `tt/Input.h` declares no member `Code/Combat/input.h` lacks, and DirectInput is declined on the roadmap's own instruction. Section 12 asks for controller behavior *where supported*; it is not supported. |

### 4.8.4 patch sites not merged

Of 760 hook sites in `docs/tt484/TTHookSites.tsv`, 76 were declined with a reason (`n/a`) and 111 are out of scope under a standing rule (`out-of-scope`). Grouped by what the site was for:

| Disposition | What the sites did | Sites | Donor files |
| --- | --- | --- | --- |
| n/a | nop out debug related functions | 12 | `tt_4.8.4/tt/tt.cpp` |
| n/a | weather hooks | 12 | `tt_4.8.4/tt/tt.cpp` |
| n/a | hook ScriptManager | 6 | `tt_4.8.4/ttinit/ttinit.cpp` |
| n/a | hooks for filehashing checks | 5 | `tt_4.8.4/tt/tt.cpp`, `tt_4.8.4/ttinit/ttinit.cpp` |
| n/a | Do not load all .mix files at startup | 4 | `tt_4.8.4/tt/tt.cpp`, `tt_4.8.4/ttinit/ttinit.cpp` |
| n/a | weather manager hooks | 4 | `tt_4.8.4/ttinit/ttinit.cpp` |
| n/a | (no stated intent) | 3 | `tt_4.8.4/tt/tt.cpp` |
| n/a | cPlayerManager hooks for player titles | 3 | `tt_4.8.4/tt/tt.cpp` |
| n/a | custom keyboard keys hook | 2 | `tt_4.8.4/tt/tt.cpp` |
| n/a | hook for VehicleBuildingDisable stuff | 2 | `tt_4.8.4/tt/tt.cpp` |
| n/a | options dialog hook | 2 | `tt_4.8.4/tt/tt.cpp` |
| n/a | patch for memory manager | 2 | `tt_4.8.4/ttinit/ttinit.cpp` |
| n/a | vehicle game obj visible change | 2 | `tt_4.8.4/tt/tt.cpp` |
| n/a | "purchase terminal ""building"" message change" | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | disable quickmatch | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | emoticons fix | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | hook DX8Wrapper::Shutdown | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | hook to allow you to select a favorite server | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | hooks for AutoPoolClass bits | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | hooks for multimon stuff | 1 | `tt_4.8.4/ttinit/ttinit.cpp` |
| n/a | nop out WWPhys::Shutdown call | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | patch to load mix files at startup for filehashing | 1 | `tt_4.8.4/ttinit/ttinit.cpp` |
| n/a | radar fix | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | remove call to StyleMgrClass::Initialize_From_INI | 1 | `tt_4.8.4/ttinit/ttinit.cpp` |
| n/a | remove call to cGameData::Onetime_Init | 1 | `tt_4.8.4/ttinit/ttinit.cpp` |
| n/a | remove calls to ToolTipMgrClass::Initialize and MenuDialogClass::Initialize | 1 | `tt_4.8.4/ttinit/ttinit.cpp` |
| n/a | spawner randomness fix | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | start button bug fix | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | stop FDS from loading at startup | 1 | `tt_4.8.4/tt/tt.cpp` |
| n/a | unload level hook | 1 | `tt_4.8.4/tt/tt.cpp` |
| out-of-scope | HUD hooks | 19 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | texture/surface hooks | 11 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | bink hooks | 6 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | shadow hooks | 6 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook for shadows | 4 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooks to make all CreateIndexBuffer calls go through shaders.dll for DX8IndexBufferClass::DX8IndexBufferClass | 4 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooks to make all SetTransform(D3DTS_PROJECTION) calls go through shaders.dll for DazzleRenderObjClass::Render_Dazzle | 4 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooks to make all vertex buffers use vertex declarations | 4 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | sound hooks | 3 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | font hook | 2 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook WW3D::Render | 2 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook for decals | 2 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook for texture mappers | 2 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooking DX8Wrapper::ZNear/ZFar | 2 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | CameraClass::Update_Frustum hook | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | MeshModelClass destroy patch | 1 | `tt_4.8.4/tt/tt.cpp` |
| out-of-scope | Shaders.dll level end hook | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | These are the addresses of Registry_Load, which is called after Init. | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | disable MessageWindowClass 3D bits | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | disable Overlay3DGameModeClass | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | disable OverlayGameModeClass | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook CameraClass::Apply | 1 | `tt_4.8.4/tt/tt.cpp` |
| out-of-scope | hook DX8Wrapper::Flip_To_Primary | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook DX8Wrapper::Get_Render_Target_Resolution | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook DX8Wrapper::Set_Device_Resoltion | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook DX8Wrapper::Set_Light_Environment | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook HLodClass::Render for per-subobject lighting | 1 | `tt_4.8.4/tt/tt.cpp` |
| out-of-scope | hook PhysClass::Update_Sun_Status | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook PhysicsSceneClass::Customized_Render | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook PhysicsSceneClass::Get_Sun_Light_Vector | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook Render2DClass::Render | 1 | `tt_4.8.4/tt/tt.cpp` |
| out-of-scope | hook Render2DSentenceClass::Render | 1 | `tt_4.8.4/tt/tt.cpp` |
| out-of-scope | hook WW3D::Begin_Render | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook WW3D::Create_Texture_Filter | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook WW3D::End_Render | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook WW3D::Get_Texture_Bitdepth | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook WW3D::Init | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook WW3D::Registry_Load_Render_Device | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook WW3D::Registry_Save_Render_Device | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook for SystemSettings stuff | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook for particle emitter creation | 1 | `tt_4.8.4/tt/tt.cpp` |
| out-of-scope | hook for physics bits | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook for w3d file hash checking | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook so we own CCameraClass | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hook to catch texture loading | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooking DX8Wrapper::Create_DX8_Texture | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooking DX8Wrapper::Draw | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooking DX8Wrapper::Set_Gamma | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooking DX8Wrapper::_Create_DX8_Surface | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooking DX8Wrapper::_Create_DX8_Texture | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooks to make all GetTransform(D3DTS_PROJECTION) calls go through shaders.dll for DazzleRenderObjClass::Render | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | hooks to make all GetTransform(D3DTS_PROJECTION) calls go through shaders.dll for DazzleRenderObjClass::Render_Dazzle | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | mesh hooks | 1 | `tt_4.8.4/ttinit/shaderhooks.cpp` |
| out-of-scope | screenshot hook | 1 | `tt_4.8.4/tt/tt.cpp` |

## P07 -- the Zero Hour donor

### Donor build directories rejected outright

25 of the donor's 51 built directories are not ported. Each carries a written reason in `ZeroHourSurfaceClassification.tsv`, which a ctest entry (`zerohour_surface_classified`) refuses to let go missing.

| Directory | Classification | Why not |
| --- | --- | --- |
| `.` | NOT_RELEVANT_TO_OPENW3D | Root list file plus an STLport shim for VC6 builds. OpenW3D builds with the 2022 toolchain and has no VC6 configuration. |
| `Core` | NOT_RELEVANT_TO_OPENW3D | Interface targets and include paths only; no source of its own. Its children carry the code. |
| `Core/Libraries/Source/WWVegas` | NOT_RELEVANT_TO_OPENW3D | Aggregation target only; no source of its own. |
| `Core/Libraries/Source/WWVegas/WWStub` | NOT_RELEVANT_TO_OPENW3D | Link stubs for tools that must not pull the whole engine. A donor build artifact. |
| `Core/Tools/Babylon` | NOT_RELEVANT_TO_OPENW3D | Localization string tool for the donor's own string format. OpenW3D has Code/Strings Editor and wwtranslatedb. |
| `Core/Tools/buildVersionUpdate` | NOT_RELEVANT_TO_OPENW3D | Stamps donor build numbers. OpenW3D has Code/Tools/VerStamp. |
| `Core/Tools/timingTest` | NOT_RELEVANT_TO_OPENW3D | A microbenchmark of the donor's own timer calls. |
| `Core/Tools/versionUpdate` | NOT_RELEVANT_TO_OPENW3D | Retail patch version bookkeeping. |
| `Dependencies/Utility` | NOT_RELEVANT_TO_OPENW3D | Donor build glue (CppMacros.h, force-included). Build infrastructure, not engine code. |
| `GeneralsMD/Code` | NOT_RELEVANT_TO_OPENW3D | Interface targets and include paths only; no source of its own. |
| `GeneralsMD/Code/Libraries/Source/WWVegas` | NOT_RELEVANT_TO_OPENW3D | Aggregation target only; no source of its own. |
| `GeneralsMD/Code/Tools/GUIEdit` | NOT_RELEVANT_TO_OPENW3D | Editor for the SAGE .wnd window format, which OpenW3D does not use. |
| `GeneralsMD/Code/Tools/MapCacheBuilder` | NOT_RELEVANT_TO_OPENW3D | Builds a cache of SAGE .map files. OpenW3D levels are W3D/LevelEdit assets. |
| `GeneralsMD/Code/Tools/WorldBuilder` | NOT_RELEVANT_TO_OPENW3D | SAGE terrain map editor. OpenW3D authors levels in LevelEdit against W3D geometry, which the roadmap requires preserving. |
| `resources` | NOT_RELEVANT_TO_OPENW3D | Donor Win32 resource scripts and icons for its own executables. |
| `Core/Libraries/Source/EABrowserDispatch` | RETAIL_INFRASTRUCTURE_REJECT | Embedded Internet Explorer COM dispatch for the retail in-game browser. |
| `Core/Libraries/Source/EABrowserEngine` | RETAIL_INFRASTRUCTURE_REJECT | The other half of the same embedded browser. |
| `Core/Libraries/Source/WWVegas/WWDownload` | RETAIL_INFRASTRUCTURE_REJECT | HTTP patch downloader for the retail auto-update service. That service is gone and OpenW3D does not ship a patcher. |
| `Core/Tools/mangler` | RETAIL_INFRASTRUCTURE_REJECT | GameSpy NAT negotiation server. |
| `Core/Tools/matchbot` | RETAIL_INFRASTRUCTURE_REJECT | GameSpy matchmaking bot. |
| `Core/Tools/wolSetup` | RETAIL_INFRASTRUCTURE_REJECT | Westwood Online account setup. |
| `GeneralsMD/Code/Libraries/Source/WWVegas/WWDownload` | RETAIL_INFRASTRUCTURE_REJECT | Target wrapper for the retail patch downloader. |
| `GeneralsMD/Code/Tools/Launcher` | RETAIL_INFRASTRUCTURE_REJECT | Retail launcher and update shell. |
| `GeneralsMD/Code/Tools/Launcher/DatGen` | RETAIL_INFRASTRUCTURE_REJECT | Generates the launcher's data file. |
| `GeneralsMD/Code/Main` | SAGE_ARCHITECTURE_REJECT | WinMain, the SAGE game loop and its ownership model. Roadmap directive: OpenW3D keeps its own game loop. |

### Parts of an accepted capability that are not taken

17 of the 40 port-matrix rows accept a capability while rejecting part of it. The checker (`zerohour_port_matrix`) fails if one of these names a subpart without saying why.

| ID | Capability | Rejected subpart, and why |
| --- | --- | --- |
| ZH-01 | Asset exclusion / residency | SAGE prototype/Thing binding: OpenW3D has definitions, not Things |
| ZH-02 | Sectional bridge renderer | faction-specific bridge unit behaviour |
| ZH-04 | Heightmap / terrain framework | replacing authored W3D world geometry: the roadmap requires preserving Renegade interiors, so the heightmap coexists with them |
| ZH-10 | Particle batching | SAGE particle INI binding |
| ZH-12 | Tracer / beam / projectile modules | SAGE weapon module binding |
| ZH-14 | Shader-manager architecture | fixed-function-only fallbacks for hardware OpenW3D no longer targets |
| ZH-15 | RTS shroud / fog-of-war | client-side shroud authority: the Commander intelligence firewall requires the server to decide |
| ZH-16 | Radar system | replacing RadarManager wholesale |
| ZH-18 | Modular draw components | SAGE Thing/Drawable ownership, which directive 0.4 and Section 13.7 both reject |
| ZH-19 | Spatial partitioning | PartitionManager as a container: OpenW3D's culling is better suited to worlds with interiors |
| ZH-24 | AI state machine / pathfinding / guard | SAGE unit ownership and faction-specific behaviour |
| ZH-25 | RTS telemetry / statistics | Zero Hour-specific scoring content |
| ZH-26 | Frame profiler | the legacy profiler, superseded by Tracy in the donor itself |
| ZH-35 | Portability shims | the MinGW toolchain itself: OpenW3D targets MSVC x86 and adopting MinGW is not on this roadmap |
| ZH-37 | Asset residency computation | SAGE map format input |
| ZH-38 | Live particle editor | the SAGE INI particle format |
| ZH-39 | Texture atlas packer | SAGE image mapping format |

### Categories of community work not accepted

From `ZeroHourCommunityEnhancementAudit.md`. These are not rejections of individual commits but of whole categories, because the code they change is either content OpenW3D does not have or architecture it does not keep.

| Category | Why not |
| --- | --- |
| Balance and content changes | Zero Hour faction balance. OpenW3D has no Zero Hour factions. |
| Generals challenge and campaign shell | Campaign structure OpenW3D does not implement. |
| GameSpy, WOL and retail patcher maintenance | Retail infrastructure that is gone. |
| Replay system and SAGE networking fixes | They fix code that is rejected architecture here; OpenW3D keeps its own networking. |
| WorldBuilder and GUIEdit improvements | Tools for formats OpenW3D does not read. |

## Decisions made inside an implementation phase

Rejections a phase made while writing code, which have no generated home. Kept in `PHASE_DECISIONS` in the tool.

### P08

| Item | Why not |
| --- | --- |
| The donor's `PrototypeClass::DeleteSelf()` | Not adopted. OpenW3D prototypes are deleted with `delete`, which is what `Free_Assets` already does; adding a second destruction protocol would leave two ways to destroy a prototype and no rule about which one a caller owes. |
| SAGE prototype / `Thing` binding in the asset manager | Rejected subpart of ZH-01. OpenW3D has definitions and its own game loop, so the donor's binding of render prototypes to `ThingTemplate` has no counterpart here and importing it would mean importing the ownership model directive 0.4 forbids keeping alongside the existing one. |
| `Core/Tools/assetcull` as a build step | Not ported as a tool. It culls an asset set offline against a SAGE map format OpenW3D does not read. The residency service does the same job at run time against the assets actually loaded, which is also the only form that can work with unmodified Renegade `.mix` content. |
| Freeing an animation that still has a live reference | Deliberately not done. `Free_All_Anims_With_Exclusion_List` releases only animations the manager alone holds. An animation something is still playing is kept whatever the keep-list says, because the alternative is a dangling pointer in a live `Animatable3DObj`. |
| Claiming prototypes permanently at startup | Deliberately not done. `Capture_Loaded_Textures` claims the startup textures and nothing claims a prototype, so every prototype is still freed on a level change. Prototypes are where the memory is, and keeping a menu-full of 3D content for the life of the process buys nothing; which prototypes are genuinely shared between levels is a decision to make against a profile, not in advance. |
| A mode-scope claim in the menu game mode | Declined. The menu could claim its own art at mode entry, but the first sweep in `CombatGameModeClass::Init` already gives menu leftovers an owner with the lifetime they already had. Two major modes writing one scope would mean two release paths for a lifetime that is already correct. |
| Releasing the mode scope in `Core_Shutdown` | Rejected in favour of `Shutdown`. `Core_Restart` calls `Core_Shutdown` without ever running `Init` again, so releasing there would empty the mode scope for the rest of the session with nothing left to re-claim it. |
| Naming textures, materials and world buffers in the exclusion list | Rejected. The exclusion list matches w3d file names and has no way to answer for an asset that has none, so `Build_Retained_List` emits only prototypes, hierarchy trees and animations. Those kinds are retained the way they always have been, by reference count, via `Release_Unused_Textures`. |

### P09

| Item | Why not |
| --- | --- |
| The chipset / vendor / driver-version table | Not adopted. `W3DShaderManager` picked its implementations from a list of card names and driver revisions, which was how a 2001 title survived contemporary hardware and is now a table that answers wrongly for every card released since. `Detect_Tier` reads D3D9 caps instead, and a program declines a tier it cannot run on rather than being excluded by name. |
| `.vso` files and DWORD shader handles | Rejected. `LoadAndCreateD3DShader` and the `IDirect3DTexture8` surfaces around it are the D3D8 API the roadmap rules out. A program that wants programmable hardware asks the tier and builds real D3D9 resources in its own `Init`. |
| The screen filters -- motion blur, black and white, cross fade | Not ported. They are SAGE presentation effects driven by render-to-texture, not material programs, and none of them appears in the Section 15 pipeline list. Bringing them across would import the D3D8 render-target code with them for effects nothing in Renegade asks for. |
| Raw texture-stage pointers | Changed rather than ported. The donor stored bare `TextureClass *` in its stage array and relied on the caller outliving the draw, which holds right up until a level release happens between staging and drawing. The stages hold references. |
| Registering the listed pipelines before their systems exist | Deliberately not done. Terrain, roads, bridges, water and foliage have nothing to draw until the terrain framework lands, and ghost tint and status markers wait on the Commander work. Each registers itself when its system arrives; until then `Is_Supported` is false and `Get_Pass_Count` is zero. |
