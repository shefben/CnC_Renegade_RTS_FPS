# WIP Features

Two or three sentences per in-flight item, ending with the next exact action.
Detail lives in `docs/`.

---

## P04 leftovers carried forward

P04 itself is closed (see `completed_features.md`, entries P04-A..P04-AO).
Two threads outlive it and must not be lost: `jfwcust.cpp`'s
`Do_Objectives_Dlg`, the single blocked call left anywhere in the in-scope set
and the reason that file stands at 89 of 90, which belongs to Phase 5 dialog
work; and two `docs/tt484/TTHookSites.tsv` rows still open from P03 -- the
C4/repair-bay arc fix and TT's ladder collision-group change -- both of which
need in-game evidence rather than more reading.

Next exact action: begin **P05** (roadmap Section 11), whose acceptance is
that the TT parity matrix carries no `UNREVIEWED` or unexplained `MISSING`
entry. Start by regenerating the matrix and reading what it still calls
unreviewed, since P04 resolved a large number of its rows through
`docs/tt484/TTScriptApiRenames.tsv`.

---

P01, P02 and P04 are complete (see `completed_features.md`). The backlog is in
`unstarted_features.md`.

---

## Working notes that cost a session each

Assembling a generated `.cpp` from parts: write the parts with the Write tool
and concatenate them with Python opened `newline=''`. `cat a b > c` in this
shell silently drops blank lines and any line carrying an inline
`/*comment*/`, which produced a file that looked like a compiler bug. Bash
heredocs are worse: a `<<'PY'` heredoc carrying a Python script silently
mangled a TSV, deleting fifty-one lines it never touched. Put any non-trivial
script in a file with the Write tool and run the file.

`docs/tt484/TTScriptApiGap.tsv` is generated by `tools/tt484/apigap_tsv.py`,
which writes the file itself -- do not redirect its stdout over it. Anything
resolved by a seam, by a rename, or by a decision not to add a command must be
written into `TTScriptApiRenames.tsv`, or the next regeneration reverts it.

`b.bat` must be invoked through the PowerShell tool
(`cmd /c "...\b.bat renegade"`); through Bash the argument never reaches the
batch file. The `vswhere.exe` warning it prints first is harmless.

---

## Reference: pinned baselines (not a lifecycle entry)

OpenW3D `b970f592282f89c7a4a2f86d54c42c1dafe7c388` on `main` (user-authorized
deviation from directive 0.2). TT donor `tt_4.8.4/`. Zero Hour donor submodule
`9fee97a1792efc50edfe27300581970b4c022951`, `Core` + `GeneralsMD` only. Canonical
build `build/win32` (x86); `build/win` (x64) secondary. Game data at
`C:\Westwood\Renegade_full` -- assets only, its binaries are out of scope.
Builds must run through `vcvarsall.bat x86`; this machine's system-wide `INCLUDE`
points at Visual Studio 6 and a plain shell compiles against the VC98 CRT, whose
errors look like broken source rather than a bad environment.
